import place from './FakeData';

const fakeData = [...place];

const shuffle = a => {
    for (let i = 0; i < a.length; i++) {
        const element = a[i];
        
    }
}

shuffle(fakeData);

export default fakeData;